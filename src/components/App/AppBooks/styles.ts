import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  rootError: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 10,
  },
  rootBooks: {
    flex: 1,
  },
});
